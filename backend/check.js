import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const reports = await prisma.faultReport.findMany(); console.log(reports); } main().catch(console.error).finally(() => prisma.$disconnect());
